<div class="row">
    <div class="col-md-12">
        <h2>Students<a href="/students/add" class="btn btn-success pull-right">Add New Student</a></h2>
        <hr />
        <div class="table-responsive">
            <table class="table">
                <thead>
                    <tr>
                        <th>Student&nbsp;ID</th>
                        <th>Full Name</th>
                        <th>Email</th>
                        <th>Address</th>
                        <th>Expected&nbsp;Credential</th>
                        <th>Status</th>
                        <th>Program</th>
                        <th>Registration&nbsp;Date</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each students}}
                    <tr>
                        <td><a href="/student/{{studentID}}">{{studentID}}</td>
                        <td>{{firstName}} {{lastName}}</td>
                        <td><a href="mailto:{{email}}">{{email}}</a></td>
                        <td>{{addressStreet}} <br> {{addressCity}}, {{addressState}} <br>{{addressPostal}}</td>
                        <td><a href="/students?credential={{expectedCredential}}">{{expectedCredential}}</a></td>
                        <td><a href="/students?status={{status}}">{{status}}</a></td>
                        <td><a href="/students?program={{program}}">{{program}}</a></td>
                        <td>{{registrationDate}}</td>
                        <td><a class="btn btn-danger" href="/students/delete/{{studentID}}">remove</a></td
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>
    {{#unless students}}
    <div class="col-md-12 text-center">
        <strong>{{message}}</strong> 
    </div>
    {{/unless}}
</div>